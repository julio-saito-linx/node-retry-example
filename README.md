# node-retry-example
--------------------

## with azk

```sh
# enter on shell
azk shell

# execute node
azk shell --command='npm install'
azk shell --command='node index.js'
```

## with node instaled

```sh
npm install
node index.js
```
